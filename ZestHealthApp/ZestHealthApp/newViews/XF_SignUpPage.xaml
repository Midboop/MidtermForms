<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="ZestHealthApp.newViews.XF_SignUpPage">
    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="Emailentery" Placeholder="Username" Text="{Binding Email}" Keyboard="Email"    
                 />
            <Entry x:Name="Nameentrery" Placeholder="Name" Text="{Binding Name}" 
                   />
            <Entry x:Name="passwordentery" Placeholder="Password" Text="{Binding Password}"     
                   IsPassword="True"/>

            <Button x:Name="signup" Text="SignUp" Command="{Binding SignUpCommand}" HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>