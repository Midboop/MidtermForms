<?xml version="1.0" encoding="utf-8" ?>
                                                            <!--Tab 2-->
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lottie="clr-namespace:Lottie.Forms;assembly=Lottie.Forms"
             mc:Ignorable="d"
             x:Class="ZestHealthApp.Pages.RecipeTabPages.RecipeInstructions">
    <ContentView.Content>
        <StackLayout>
            <ScrollView HeightRequest="330">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"/>
                    </Grid.RowDefinitions>
                    <StackLayout x:Name="stackLayout" Grid.Column="0" Grid.ColumnSpan="2" >
                        <ImageButton Source="addButtonStatic2.jpg" HorizontalOptions="Center" BackgroundColor="Transparent" Clicked="AddButton_OnClick" HeightRequest="40" WidthRequest="40"/>
                    </StackLayout>
                    <lottie:AnimationView Grid.Column="1" x:Name="SaveAnim" AutoPlay="True" Loop ="True" IsVisible="False" Animation="savewiggle.json" HorizontalOptions="End" OnClick="SaveAnim_OnClick" />
                </Grid>
            </ScrollView>
        </StackLayout>
    </ContentView.Content>
</ContentView>